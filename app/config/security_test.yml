security:
    firewalls:
        pinbarrest:
            pattern:                        ^/api/rest/latest/pinbars
            provider:                       oro_user
            api:                            false
            http_basic:
                realm: "Secured REST Area"
