{% extends bap.layout is defined ? bap.layout : 'AcmeDemoFlexibleEntityBundle::layout.html.twig' %}
{% import 'OroUserBundle::macros.html.twig' as macros %}

{% set title = form.vars.value.id ? 'Edit Address' : 'Add Address' %}

{% block content %}
    <div class="container-fluid">
        <form action="{{ form.vars.value.id ? path('acme_demo_service_address_edit', { id: form.vars.value.id, back: app.request.get('back') }) : path('acme_demo_service_address_create', { back: app.request.get('back') }) }}" {{ form_enctype(form) }} method="post">
            <div class="navigation clearfix navbar-extra navbar-extra-right">
                <div class="row">
                    <div class="pull-left">
                        <div class="well-small">
                            <div class="customer-content">
                                <h1 class="address-title">{{ form.vars.value.id ? 'Edit Address' : 'New Address' }}</h1>
                                <ul class="inline">
                                    <li>{{ 'Created'|trans }}: {{ form.vars.value.createdAt ? macros.time(form.vars.value.createdAt) : 'N/A' }}</li>
                                    <li>{{ 'Updated'|trans }}: {{ form.vars.value.updatedAt ? macros.time(form.vars.value.updatedAt) : 'N/A' }}</li>
                                </ul>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="layout-content">
                <div id="navbarExample" class="navbar navbar-static scrollspy-nav">
                    <div class="navbar-inner">
                        <div class="container-fluid" style="width: auto;">
                            <ul class="nav">
                                <li class="active"><a href="#scroll-main">{{ 'Overview'|trans }}</a></li>
                            </ul>
                        </div>
                    </div>
                </div>
                <div class="clearfix-oro">
                    <div class="customer-info-actions container-fluid well-small">
                        <div class="pull-right">
                            <button type="submit" class="btn btn-primary">{{ 'Save'|trans }}</button>
                        </div>
                        {% if form_errors(form) %}
                            <div class="pull-left alert alert-error">
                                <button class="close" type="button" data-dismiss="alert">×</button>
                                {{ form_errors(form) }}
                            </div>
                        {% endif %}
                    </div>
                    <div data-spy="scroll" data-target="#navbarExample" data-offset="50" class="scrollspy container-fluid">
                        <h4 id="scroll-main" class="scrollspy-title">{{ 'Overview'|trans }}</h4>
                        <div class="row-fluid">
                            <fieldset class="form-horizontal span6">
                                {{ form_rest(form) }}
                            </fieldset>
                        </div>
                    </div>
                </div>
            </div>
        </form>
    </div>
{% endblock content %}
